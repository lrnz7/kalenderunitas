name: kalender_unitas
description: "Kalender Unitas Sistem Informasi"
publish_to: 'none'

version: 1.3.3+1

environment:
  sdk: '>=3.5.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.2
  http: ^1.1.0
  firebase_core: ^3.15.2
  cloud_firestore: ^5.6.11

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
  
  assets:
    - assets/events.json
    - assets/holidays.json        # ← TAMBAH INI
    - assets/images/logo_unitas.png
    - assets/images/logo_unitas_white.png